import styled from 'styled-components';

export const WineBottle = styled.img.attrs({
  src: '/static/wine_bottle.png',
})`
  position: absolute;
  bottom: 0;
  left: 0;
  max-width: 20.125rem;
  transform: translateX(1vw);
`;
